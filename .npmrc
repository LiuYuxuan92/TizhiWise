# 关闭范围依赖，统一使用精确版本，保证可复现安装
save-exact=true
# 严格的 peer 依赖检查；如遇第三方包声明不全可临时放宽
strict-peer-dependencies=false
# 允许 workspace 包通过 link 解析
link-workspace-packages=true
# 构建脚本白名单（pnpm 10 默认禁止依赖运行 postinstall 脚本）
enable-pre-post-scripts=true
